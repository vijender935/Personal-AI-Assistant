import React,{useState} from "react";
import {LockKeyhole,Mail,UserRound,LogIn,ShieldCheck,Sparkles} from "lucide-react";
import {apiFetch} from "../api";
export default function AuthScreen({API,mode,onAuthenticated}){
 const setup=mode==="setup"; const [name,setName]=useState(""); const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [busy,setBusy]=useState(false); const [error,setError]=useState("");
 async function submit(e){e.preventDefault();if(busy)return;setError("");setBusy(true);try{const r=await apiFetch(API+(setup?"/api/v1/auth/setup":"/api/v1/auth/login"),{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(setup?{display_name:name,email,password}:{email,password})});const d=await r.json().catch(()=>({}));if(!r.ok){setError(d.detail||"Authentication failed.");return}onAuthenticated(d.account)}catch{setError("Cannot connect to Personal AI backend.")}finally{setBusy(false)}}
 return <div className="auth-page"><div className="auth-card-new"><div className="auth-logo"><Sparkles size={28}/></div><h1>{setup?"Create your Personal AI account":"Welcome back"}</h1><p>{setup?"Create the one account used to protect your personal assistant.":"Sign in to access your personal assistant."}</p><form onSubmit={submit}>{setup&&<label><span>Name</span><div className="auth-input"><UserRound size={17}/><input autoComplete="name" value={name} onChange={e=>setName(e.target.value)} placeholder="Your name" required/></div></label>}<label><span>Email</span><div className="auth-input"><Mail size={17}/><input type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" required/></div></label><label><span>Password</span><div className="auth-input"><LockKeyhole size={17}/><input type="password" autoComplete={setup?"new-password":"current-password"} value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 8 characters" minLength={8} required/></div></label>{error&&<div className="auth-error">{error}</div>}<button className="auth-primary" disabled={busy}><LogIn size={17}/>{busy?(setup?"Creating…":"Signing in…"):(setup?"Create account":"Sign in")}</button></form><div className="auth-security"><ShieldCheck size={17}/><span>Single account · password protected · persistent session</span></div></div></div>
}
