import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.vijender935.personalassistant",
  appName: "Personal AI Assistant",
  webDir: "dist",
  bundledWebRuntime: false
};

export default config;
